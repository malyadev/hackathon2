<form method="post" action="{{ path('prescription_drug_delete', {'id': prescription_drug.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ prescription_drug.id) }}">
    <button class="btn">Delete</button>
</form>
